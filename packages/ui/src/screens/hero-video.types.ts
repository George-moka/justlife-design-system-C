/** Shared props for the platform-split funnel hero video. */
export interface HeroVideoProps {
  /** Remote MP4 (Justlife's CDN loop for the service). */
  source: string;
  /** Poster/fallback colour behind the frame while it loads. */
  backgroundColor: string;
}
