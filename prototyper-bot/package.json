{
  "name": "justlife-ds-prototyper-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Slack bot: PMs describe a component idea in a DM, an agent builds it against the Justlife design system in a sandbox, and Chromatic returns a live preview link. No GitHub or terminal for the PM.",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.207",
    "@anthropic-ai/sdk": "^0.111.0",
    "@octokit/rest": "^21.0.0",
    "@slack/bolt": "^4.2.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
